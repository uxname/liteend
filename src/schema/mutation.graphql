type Mutation {
    echo(text: String!): String!
    register(username: String!, password: String!): AuthResult!
    login(username: String!, password: String!): AuthResult!
    changePassword(password: String!, newPassword: String!): AuthResult!
}
