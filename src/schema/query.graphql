type Query {
    echo(text: String!): String! @cost(complexity: 5)
    error: Int
    whoami: Account!
}

type AccountSession {
    id: Int!
    createdAt:  Date!
    updatedAt:  Date!
    account:    Account!
    ipAddr:     String!
    userAgent:  String
    expiresAt:  Date!
}
